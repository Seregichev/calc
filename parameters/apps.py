from django.apps import AppConfig

class ParametersAppConfig(AppConfig):
    name = "parameters" # Здесь указываем исходное имя приложения
    verbose_name = "Надстроечные параметры" # А здесь, имя которое необходимо отобразить в админке