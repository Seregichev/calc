from django.apps import AppConfig

class CalculationsAppConfig(AppConfig):
    name = "calculations" # Здесь указываем исходное имя приложения
    verbose_name = "Расчеты" # А здесь, имя которое необходимо отобразить в админке